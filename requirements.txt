# Bigkas Backend Dependencies

# Core framework
fastapi>=0.104.0

# ASGI server with standard extras (includes websockets, httptools, etc.)
uvicorn[standard]>=0.24.0

# Required for file uploads (UploadFile)
python-multipart>=0.0.6

# Pydantic for data validation (included with FastAPI, but explicit for clarity)
pydantic>=2.0.0

# Future dependencies (uncomment as needed):
# python-jose[cryptography]>=3.3.0  # JWT authentication
# passlib[bcrypt]>=1.7.4            # Password hashing
# sqlalchemy>=2.0.0                 # Database ORM
# asyncpg>=0.29.0                   # PostgreSQL async driver
# python-dotenv>=1.0.0              # Environment variable loading
# aiofiles>=23.0.0                  # Async file operations
